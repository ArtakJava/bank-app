package org.example.menu.mainMenu;

import org.example.menu.MenuOption;
import org.example.menu.MenuOptionManager;

public class AccountMenuOption extends MenuOption {

    public AccountMenuOption(MenuOption root) {
        super(root, MenuOptionManager.ACCOUNT_MENU);
    }
}