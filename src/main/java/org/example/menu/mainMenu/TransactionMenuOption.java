package org.example.menu.mainMenu;

import org.example.menu.MenuOption;
import org.example.menu.MenuOptionManager;

public class TransactionMenuOption extends MenuOption {

    public TransactionMenuOption(MenuOption root) {
        super(root, MenuOptionManager.TRANSACTION_MENU);
    }
}